files:
  "/etc/php.d/project.ini" :
    mode: "000644"
    owner: root
    group: root
    content: |
      file_uploads=On
      memory_limit=500M
      upload_max_filesize=500M
      max_execution_time=600
